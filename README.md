# The Lost Force 🧱⚔️

**The Lost Force** is a project that manages data about Lego figures, where each figure represents a specific programming language or dialect. 

The core idea is to track my learning progress. As I advance in a language, I add "studs" to its corresponding Lego figure. By looking at the number of studs, I can easily visualize my progress and proficiency!

## 🛠️ Technology Stack
- **Language:** C++ (Object-Oriented Programming featuring `Character`, `Jedi`, and `Sith` classes)
- **Build System:** Makefile

## 🚀 Getting Started

### Prerequisites
To compile and run this application, you need:
- A C++ compiler (e.g., `g++`)
- `make`

### Compilation
Navigate to the project root directory where the `Makefile` is located, and run:

```bash
make

This will compile the source files and generate an executable named galaxy.
Running the Application

After a successful build, start the application by running:
Bash

./galaxy
